[tool.ruff]
line-length = 120
exclude = ["slskd-*", "slskr-*", "previews", "spotify-soulseek-web"]

[tool.ruff.lint.per-file-ignores]
"spotify_web.py" = ["BLE001", "S110", "DTZ005"]
"local_config.py" = ["BLE001", "S110"]

[tool.pytest.ini_options]
testpaths = ["tests"]
